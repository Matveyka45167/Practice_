def permutation(word, p):
    n = len(word)
    res = [' '] * n
    for i in range(n):
        res[p[i] - 1] = word[i]
    return ''.join(res)
n, k = map(int, input().split())
p = list(map(int, input().split()))
s = input()
